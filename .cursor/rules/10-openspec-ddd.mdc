---
description: OpenSpec 产物中的 DDD 约束
globs:
  - "docs/openspec/**"
---

# 规则

- 先判定 change type：`business` / `technical` / `hybrid`。
- **business / hybrid 的业务部分**：`design.md` 必须包含「领域设计」小节，至少说明：
  - 本次采用简化三层或完整 DDD 分层
  - 选择理由
  - 涉及的领域模型变更点
  - 关联 `docs/domain/developing` 条目
- **technical / UI 壳层 change**：`design.md` 写「技术方案」与分层裁剪决策；不得为了满足模板而虚构领域模型或新限界上下文。
- `spec.md` 中业务 Requirement 术语优先使用 `docs/domain/established/ubiquitous-language.md`；新增业务术语写入 `docs/domain/developing/ubiquitous-language.md`。UI / 技术实现词（如 App Shell、MainLayout、Design Token、AuthStore、具体框架 API）不写入 DDD 统一语言。
- 业务 capability 命名应尽量与限界上下文一致（kebab-case）；技术 capability 可按架构能力命名，但不得进入 `docs/domain/established/context-map.md`。
- 若变更涉及下线能力，使用 `REMOVED Requirements` 并触发人工确认
