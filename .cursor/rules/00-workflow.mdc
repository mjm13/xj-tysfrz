---
description: DDD + OpenSpec + Superpowers 的主流程规则与门禁
alwaysApply: true
---

# Change 分类

每个 change 先判定类型，再决定文档深度。**禁止**把纯技术基建写成新的限界上下文。

| 类型 | 定义 | 示例 | 领域文档（`docs/domain`） | 主要文档 |
| --- | --- | --- | --- | --- |
| **业务 change** | 引入或变更业务能力、规则、术语、上下文边界 | 指标填报、审批流、权限模型 | 必须更新 `docs/domain/developing/*`（context-map / ubiquitous-language / domain-model） | OpenSpec + domain |
| **技术 change** | 工程骨架、横切能力、工具链、UI 壳层；无新业务规则 | `springboot-backend-skeleton`、`platform-shell`（布局 / 路由 / Design Token） | **不写入 `docs/domain` 的 context-map / domain-model / ubiquitous-language**；如影响业务能力，只在已有业务上下文中记录业务语义变化，不记录框架/布局实现 | OpenSpec `design.md` + ADR / `docs/architecture.md` / `docs/capability-map.md` |
| **混合 change** | 既有基建又有业务规则 | 后端鉴权 + RBAC 策略 | 仅建模业务部分；基建写入 ADR / design | 两者兼有 |

**技术 change 识别信号**（满足多数即可归类为技术 change）：
- 需求描述为「项目骨架 / 脚手架 / 初始化 / 升级依赖 / CI 模板」
- OpenSpec capability 以 `project-structure`、`logging`、`health-monitoring` 等横切能力为主
- 能力是 UI 壳层、布局、路由、Design Token、前端状态管理等平台支撑（如 `platform-shell`）
- 不产生新的业务术语或聚合根
- 主要交付物是可运行工程结构与通用中间件配置

# 需求分级（入口闸门，先分级再决定走多重的流程）

**禁止一刀切**：不是所有需求都进 OpenSpec 全管道。先按复杂度分档，再选流程。

| 档位 | 适用 | 流程 | 持久化 |
| --- | --- | --- | --- |
| 🟢 轻量 | 配置/文案/样式变更、简单 CRUD、独立前端页面 | **Plan Mode**：AI 出方案 → 人审 → 执行；不进 OpenSpec | 方案落 `docs/plans/`（值得留则留） |
| 🟡 中等 | 单一上下文内常规功能、弱不变量 | **Plan Mode + 可选 Superpowers**（含可测逻辑时）；需沉淀领域知识则补 `docs/domain/developing/*` | 计划落 `docs/plans/`；必要时 ADR |
| 🔴 核心 | 资金链路、权限模型、复杂业务规则、跨上下文、多聚合 | **完整 OpenSpec + Superpowers**（`openspec-superpowers-apply`） | OpenSpec specs + `docs/domain` |

**分级原则**：
- 拿不准时**先降一档**，发现复杂度超预期再升档（升档成本远低于过度设计成本）
- 改配置走 🟢，别写 40 分钟 Spec
- 只有 🔴 才值得 OpenSpec 的持久化与同步成本；本项目无硬性审计需求，OpenSpec 仅服务核心业务的知识沉淀

**🧪 探针档（spike，正交于复杂度档）**：
- 适用：链路严重不清、技术可行性未知、需求无法描述完整时，**先探路**
- 性质：**一次性、可丢弃**的实验，目的只有两个——摸通端到端链路、把「未知」变成「可回答的问题」
- 流程：走 🟢 的轻量节奏，方案与发现落 `docs/plans/<spike-name>.md`
- 铁律：**spike 代码不得直接当成果交付**；探针结束后产出喂给正式 change（🟡/🔴）重做或重构
- 退出条件：未知已澄清、链路已验证 → 升档为正式 change；或证实不可行 → 记录结论后丢弃

# 迭代切片（需求无法一次描述完时的默认姿态）

**现实：绝大部分需求无法一次描述完所有链路，也很难一次做完。** 因此默认按「纵向切片」推进，而非「一次做完整功能」。

**三条心法：**
1. **切片优先**：工作单元是「打通一条端到端链路的薄切片」，不是「完整模块/功能」。一个 OpenSpec change = 一个切片，**不是**一个模块。change 命名切片化（如 `identity-basic-ingest-from-hr`，而非 `identity-basic`）。
2. **活文档增量**：`spec.md`、`data-dictionary.md`、`docs/domain/*` 都是活文档，随每个 change 逐次生长；第一版不全是正常的，靠 `archive → sync-knowledge` 循环长期累积。
3. **显式未知**：想不清的链路**显式 park**，不塞进 design 假装确定。每个 proposal 必含三段（见「联动门禁」）：
   - **In Scope**：本切片要打通的链路与 AC
   - **Out of Scope**：明确不在本次范围内
   - **Open Questions / Deferred**：尚未想清、需后续切片或 spike 解决的链路

**与 AC 门禁的关系**：门禁只约束 **本切片的 In Scope AC**；标记为 `Deferred` 的 AC 不阻塞当前切片勾选完成（详见「真相源与冲突裁决」）。

# 知识沉淀（与流程档位正交）

**沉淀什么，与走多重的流程无关。** 即使是 🟢 改配置，只要命中下面三问，也必须沉淀（成本通常一行）。

**沉淀三问（任一为「是」即沉淀）：**
1. 是业务规则 / 不变量吗？（如「投标截止后不可撤回」「扣费不退款」）→ `docs/domain`（ubiquitous-language / domain-model）
2. 是非显而易见的决策及其原因吗？→ `docs/decisions`（ADR）
3. 是数据库 / 字段的业务语义、状态流转、约束理由吗？→ `docs/domain/.../data-dictionary.md`

**不沉淀：**
- 与代码重叠的实现细节——**代码即文档**，抄进 docs 必然漂移
- 一次性琐碎改动
- UI / 技术实现词不写入 DDD 统一语言或领域模型：如 `App Shell`、`MainLayout`、`Design Token`、`AuthStore`、框架类名、拦截器、具体鉴权库 API。它们应放在 `docs/architecture.md`、ADR、OpenSpec 技术 spec 或 `docs/capability-map.md`

**数据库三层（结构与语义分离）：**
- 结构真相 = Flyway 迁移脚本（可执行，不抄进 docs）
- 实时读取 = MySQL MCP（只读）
- 业务语义 = 数据字典（字段业务含义、状态机、约束理由——DDL 里看不出的代码外知识）

**能力追溯索引（按需求发布节点维护，三档通用）：**
- `docs/capability-map.md` 是模块级薄索引，只记录「模块 / moduleKey / 前端入口 / 后端能力 / 相关表 / Demo」指针，不复制子菜单、路由明细或 DDL
- 触发条件（任一命中即需更新）：新增/下线/重命名业务模块、变更前端 `moduleKey`、新增模块相关业务表、新增模块根路由
- 维护节点按档位（发布即收尾）：
  - 🔴：在 `sync-knowledge` 收尾时检查更新
  - 🟢 / 🟡：在该需求完成、人确认收尾时直接检查更新（这两档不走 `sync-knowledge`，不得因此漏掉）
- 数据表字段语义仍写 `data-dictionary.md`；表结构仍以 Flyway 为唯一真相

**developing vs established 的写入规则：**
- 🔴（OpenSpec change 在飞行中）：先写 `developing/`，归档后由 `sync-knowledge` 提升到 `established/`
- 🟢 / 🟡（无 OpenSpec change，完成即既成事实）：人确认后**直接写 `established/`**
- `developing/` 仅用于隔离「未归档 OpenSpec change」，不阻塞轻量需求的沉淀

# 主流程（🔴 档完整流水线；🟢🟡 见上表，到对应档位即停）

当用户提出 🔴 核心需求时，执行以下流水线：

1. `explore`：用 `/opsx:explore` 探索问题；**业务 change** 补齐 `docs/domain/developing/*` 条目，**技术 change** 跳过 domain 新建，必要时写 ADR 草案
2. `propose`：用 `/opsx:propose` 生成 proposal/design/tasks
3. `apply`：用 **openspec-superpowers-apply** 实现任务（OpenSpec 管 tasks 进度，Superpowers 管 TDD/验证/评审；禁止裸跑 apply）
4. `verify`：执行测试、验证与代码评审
5. `sync`：用 `/opsx:sync` 同步 delta specs 到主 specs
6. `archive`：用 `/opsx:archive` 归档 change
7. `sync-knowledge`：以需求发布为节点，将有效知识回灌 docs 与 ADR，并检查 `capability-map.md` / `data-dictionary.md`（技术 / UI 壳层 change 以 ADR、架构文档、能力索引为主，不回灌伪领域条目）

# 文档状态模型

- 需求：`inbox -> in-change(<name>) -> shipped/dropped`
- 领域条目：`developing -> established`
- ADR：`proposed -> accepted -> superseded/deprecated`

**需求文档物理位置随状态迁移**（frontmatter `status` 与目录必须一致）：

| status | 目录 | 说明 |
| --- | --- | --- |
| `inbox` / `in-change` | `docs/requirements/inbox/` | 待办与进行中，统一留在 inbox |
| `shipped` | `docs/requirements/shipped/` | 完成后**移入**，不与 inbox 混放 |
| `dropped` | `docs/requirements/dropped/` | 放弃后**移入**（目录不存在则创建） |

- 迁移用 `git mv` 保留历史；移动后用 Grep 检查并修正引用该路径的其他文档（plans/ADR/README 等），避免死链
- `docs/requirements/requirements-template.md` 始终留在 `requirements/` 根，不参与迁移

# 联动门禁

- 每个 change 建议先建立独立 worktree 与分支 `change/<name>`
- propose 前必须在 proposal 中标注 change 类型（业务 / 技术 / 混合）
- propose 前统一语言：**业务 / 混合 change 的业务术语**必须具备 `ubiquitous-language` 条目；**技术 / UI 壳层 change** 不写入 DDD 统一语言，除非该术语会成为业务人员也使用的领域语言
- **proposal 必含范围三段**：`In Scope` / `Out of Scope` / `Open Questions & Deferred`。想不清的链路写进第三段显式 park，禁止塞进 design 假装确定
- apply 前 design 必须包含：
  - **业务 / 混合 change**：领域设计小节 + 分层裁剪决策
  - **技术 change**：技术方案小节（栈选型、目录结构、横切能力清单）+ 分层裁剪决策；**不要求**虚构领域模型
- 技术 / UI 壳层 change 的关键决策写入 `docs/decisions/`（ADR）、`docs/architecture.md` 或 `docs/capability-map.md`，不写 `context-map` 新 context，也不在 `domain-model.md` 虚构无聚合的模型
- 开发中的领域内容只能写入 `docs/domain/developing/*`；仅在 `sync -> archive -> sync-knowledge` 后可提升到 `docs/domain/established/*`
- 收尾仅两条路径：
  - 完成：`sync -> archive -> sync-knowledge`；收尾时检查能力追溯索引、数据字典与必要 ADR，**输出「人工验收说明」**，将需求 `status` 置 `shipped` 并 `git mv` 到 `docs/requirements/shipped/`
  - 放弃：`abandon-change`；收尾时将需求 `status` 置 `dropped` 并 `git mv` 到 `docs/requirements/dropped/`
- 🟢 / 🟡 收尾同样必须**输出「人工验收说明」**（见下方专节）
- 未归档 change 不得写入 `docs/domain/established/*`

# 真相源与冲突裁决（测试 > Spec）

Spec/design 是自然语言意图，没有裁判机制；测试可执行、有 pass/fail。两者冲突时：

- **实现期以测试为准**：任务完成与否由验证/测试裁决，不由 Spec 自洽与否裁决
- **测试暴露 Spec 错误时不得静默跳过**：暂停 → 先改 `design.md`/spec → 再继续实现
- **Spec 只锁 What，不锁 How**：边界、不变量、验收标准（AC）该写死；技术选型、实现路径、代码结构交给模型，给信息而非指令
- 每条验收标准（AC）应能对应到测试；🔴 档 AC 无对应测试不得勾选任务
- **AC 门禁只约束「本切片 In Scope 的 AC」**：标记为 `Deferred` / `Out of Scope` 的 AC 不阻塞当前切片完成；「本切片没做完」是合法状态，留给后续切片，而非违规

# 人工验收说明（每次收尾必产出，三档通用）

**问题：交给 AI 编码后，人往往不知道改了哪些东西、从哪验起。** 因此**任何档位**的需求收尾时，AI 必须输出一段「人工验收说明」，让人能脱离代码、按场景点验。**禁止只说「已完成、测试通过」就收尾。**

**落地位置（必须持久化，不能只在聊天里说）：**
- 追加到对应需求文件的 `# 验收记录` 段：`docs/requirements/inbox/<req>.md`（完成后随状态迁移到 `shipped/`）
- 若该需求未走 OpenSpec（🟢/🟡）只有 `docs/plans/` 计划文档，则追加到该计划文档
- 多切片场景**追加不覆盖**：每个切片一条记录，保留历史
- 同时在收尾摘要里复述一份，方便当场验收

**验收说明模板（写入需求文件的 `# 验收记录`，并在收尾摘要中复述）：**

```markdown
## 人工验收说明（Acceptance Note）

- 涉及菜单 / 模块：<moduleKey + 菜单路径，如 identity-permission → /identity/permission；无界面则写「后端 API / 无界面」>
- 改了什么功能：<一句话能力描述>
- 验收场景（人可复现）：
  1. GIVEN <前置> WHEN <操作> THEN <可观察结果>
  2. ...
- 手动验证步骤：<点哪个菜单、输入什么、预期看到什么；后端给 curl/接口示例与预期响应>
- 自动化覆盖：<对应测试名 / 验证命令>；未覆盖的标注「需人工点验」
- 本次范围外 / Deferred：<明确列出本切片未做的链路，对齐 proposal 的 Open Questions>
```

**要点：**
- 必须点名 **改了哪个菜单/模块（moduleKey）、什么功能、什么场景**——这是人工验收的最小信息集
- 验收场景与 `Out of Scope / Deferred` 必须如实，未做的不许写成已做
- 🔴 在 `openspec-superpowers-apply` 完成模板中输出；🟢/🟡 在 Plan Mode 执行收尾时输出

# Approval Gates（必须暂停并请求用户确认）

- 破坏性数据库变更（删表/删列/改类型/数据回填）
- 新增关键外部依赖（Feign、三方 API、核心包）
- 删除或下线已上线能力
- 权限、密钥、安全策略相关变更
- 跨限界上下文的大规模架构调整
