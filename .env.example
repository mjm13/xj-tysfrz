# 复制为 .env 后填入真实值（.env 已被 git 忽略，勿提交）

# 数据库（后端 dev profile / MCP 只读连接共用）
DB_HOST=localhost
DB_PORT=3306
DB_NAME=xj_zbpt
DB_USERNAME=root
DB_PASSWORD=your-password

# 前端 API 基址（Vite）
VITE_API_BASE_URL=http://localhost:8080
