---
title: <requirement-title>
status: inbox
owner: <owner>
createdAt: <yyyy-mm-dd>
---

# 背景

描述业务背景、触发原因和目标。

# 业务目标

- 目标 1
- 目标 2

# 用例 / 用户故事

1. 作为 <角色>，我希望 <行为>，以便 <收益>。
2. 作为 <角色>，我希望 <行为>，以便 <收益>。

# 涉及限界上下文

- `<bounded-context-1>`
- `<bounded-context-2>`

# 关键领域概念（统一语言）

- `<term-a>`：定义
- `<term-b>`：定义

# 领域规则与不变量

- 规则 1
- 规则 2

# 领域事件（可选）

- `<event-name>`：触发条件与业务意义

# 非功能需求（可选）

- 性能
- 安全
- 可观测性

# 范围切分（需求无法一次做完时）

> 默认按纵向切片推进，一个切片 = 一条端到端链路 = 一个 change。

## 本切片 In Scope

- 本次要打通的链路与能力

## Out of Scope

- 明确不在本次范围内（避免 AI 自行扩张）

## Open Questions & Deferred

- 尚未想清、需后续切片或 🧪 spike 探针解决的链路与未知
- 每条尽量写成「可回答的问题」，而非模糊待办

# 验收标准

> 只为「本切片 In Scope」写 AC；Deferred 的链路不在此列。

- [ ] GIVEN ... WHEN ... THEN ...
- [ ] GIVEN ... WHEN ... THEN ...

# 验收记录（每个切片收尾追加，便于人工检查）

> 每完成一个切片 / change，AI 在收尾时在此**追加一条**（不删旧记录），让人脱离代码按场景点验。
> 若该需求未走 OpenSpec（🟢/🟡）只有 `docs/plans/` 计划文档，则把同样内容追加到计划文档。

## <切片 / change 名> — <yyyy-mm-dd>

- 涉及菜单 / 模块：<moduleKey + 路径，或「后端 API / 无界面」>
- 改了什么功能：<一句话>
- 验收场景：
  1. GIVEN ... WHEN ... THEN ...
- 手动验证步骤：<点哪个菜单、输入什么、预期看到什么；后端给接口示例与预期响应>
- 自动化覆盖：<测试名 / 命令>；需人工点验：<...>
- 本次范围外 / Deferred：<对齐上方 Open Questions>

# 备注

补充信息、外部依赖、风险等。
