# identity-access-mock Specification

## Purpose

**已退役（superseded）**。本 capability 由 `identity-access`（change `platform-user-access-control`，2026-06-21）替代。历史 Mock 登录能力不再作为生产要求。

## Requirements

（无活跃 Requirement。原 Mock 登录 / 路由守卫 / 会话持久化 / 退出登录 已迁移至 `identity-access` 与 `platform-shell`。）
